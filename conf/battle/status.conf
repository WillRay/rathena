//--------------------------------------------------------------
// rAthena Battle Configuration File
// Originally Translated by Peter Kieser <pfak@telus.net>
// Made in to plainer English by Ancyker
//--------------------------------------------------------------
// Note 1: Value is a config switch (on/off, yes/no or 1/0)
// Note 2: Value is in percents (100 means 100%)
// Note 3: Value is a bit field. If no description is given,
//         assume unit types (1: Pc, 2: Mob, 4: Pet, 8: Homun, 16: Mercenary, 128: NPC, 512: Elemental)
//--------------------------------------------------------------

// Should skill casting be cancelled when inflicted by curse/stun/sleep/etc (includes silence) (Note 3)?
status_cast_cancel: 0

// Will certain skill status-changes be removed on logout?
// 0 = (Default) Only remove status that has the SCF_NO_SAVE flag.
// 1 = Remove negative buffs (status that are flagged as debuff)
// 2 = Remove positive buffs.
// 3 = Remove all.
debuff_on_logout: 0

// Adjustment for the natural rate of resistance from status changes.
// If 50, status defense is halved, and you need twice as much stats to block
// them (eg: 200 vit to completely block stun)
pc_status_def_rate: 100
mob_status_def_rate: 100

// Maximum resistance to status changes. (100 = 100%)
// NOTE: Cards and equipment can go over this limit, so it only applies to natural resist.
pc_max_status_def: 100
mob_max_status_def: 100

//--------------------------------------------------------------
// VIT Redesign (custom)
//--------------------------------------------------------------
// Soft DEF: def2 = VIT + VIT*VIT/vit_def2_quadratic_divisor (players only). 0 disables the
// quadratic term (falls back to stock def2 = VIT).
vit_def2_quadratic_divisor: 150

// Soft MDEF: mdef2 = VIT + VIT*VIT/vit_mdef2_quadratic_divisor (players only). INT no longer
// contributes to mdef2. 0 disables the quadratic term (mdef2 becomes VIT only).
vit_mdef2_quadratic_divisor: 400

// Status resistance curve: sc_def = 10000 * VIT / (VIT + vit_sc_def_halfpoint).
// VIT == vit_sc_def_halfpoint is exactly the 50% resist point. Applies to Stun, Silence,
// Bleeding, Poison, Sleep, Blind, Confusion, and Curse (both players and monsters).
vit_sc_def_halfpoint: 60

// Flat MaxHP bonus from VIT: min(vit_flat_hp_cap, floor(VIT^1.5)), added on top of the
// existing percentage MaxHP bonus. Players only. 0 disables.
vit_flat_hp_cap: 1000
